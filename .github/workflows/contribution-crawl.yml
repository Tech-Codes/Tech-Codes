name: Generate Contribution Crawl

on:
schedule:
- cron: "0 0 * * *"
workflow_dispatch:
push:
  branches:
    -main

permissions:
contents: write

jobs:
generate:
runs-on: ubuntu-latest
timeout-minutes: 15

steps:
  - uses: actions/checkout@v4

  - name: Clone Contribution Crawl
    run: git clone https://github.com/Tech-Codes/Contribution-Crawl.git crawl

  - name: Setup Node.js
    uses: actions/setup-node@v4
    with:
      node-version: '20'

  - name: Install and build
    working-directory: crawl
    run: npm ci

  - name: Build
    working-directory: crawl
    run: npm run build

  - name: Generate Crawl
    working-directory: crawl
    run: node dist/index.js Tech-Code ..
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  - name: Commit changes
    run: |
      git config user.name "github-actions[bot]"
      git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      git add contribution-crawl-*.svg
      git diff --staged --quiet || git commit -m "Update contribution crawl"
      git push
